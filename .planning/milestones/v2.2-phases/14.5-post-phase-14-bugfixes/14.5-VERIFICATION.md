---
phase: 14.5-post-phase-14-bugfixes
verified: 2026-04-01T00:00:00Z
status: passed
score: "9/9 must-haves verified"
re_verification: false
---

# Phase 14.5: Post-Phase-14 Bug Fixes Verification Report

**Phase Goal:** Fix three bugs identified during Phase 14 VPS verification
**Verified:** 2026-04-01
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Header lastUpdated receives pipelineTimestamp (from useLatestTimestamp), not data.generated_at | VERIFIED | `frontend/src/App.tsx` line 26: `const pipelineTimestamp = timestampData?.timestamp ?? null`; line 78: `<Header lastUpdated={pipelineTimestamp}` -- uses pipeline run timestamp, not request-time generated_at |
| 2 | isStale computed against pipelineTimestamp, not request-time generated_at | VERIFIED | `frontend/src/App.tsx` lines 37-39: `const isStale = pipelineTimestamp ? Date.now() - new Date(pipelineTimestamp).getTime() > STALE_THRESHOLD_MS : false` -- references pipelineTimestamp variable |
| 3 | hasNewPredictions uses initialTimestampRef to detect new pipeline runs after page load | VERIFIED | `frontend/src/App.tsx` lines 29-34: `const initialTimestampRef = useRef<string \| null>(null)` with useEffect to capture first timestamp; lines 42-46: `hasNewPredictions` compares `timestampData.timestamp` against `initialTimestampRef.current` |
| 4 | useEasternClock localTimeFmt formatted without timeZone:'America/New_York' | VERIFIED | `frontend/src/hooks/useEasternClock.ts` lines 20-24: `const localTimeFmt = new Intl.DateTimeFormat('en-US', { hour: 'numeric', minute: '2-digit', hour12: true })` -- no timeZone property; browser locale used implicitly |
| 5 | No ' ET' suffix on the clock time string | VERIFIED | `frontend/src/hooks/useEasternClock.ts` line 96: `const timeStr = tzAbbr ? \`${localTimeFmt.format(now)} ${tzAbbr}\` : localTimeFmt.format(now)` -- uses browser-local timezone abbreviation (e.g., "MST", "PST"), not hardcoded "ET" |
| 6 | Next update display shows local-equivalent time via etRunHourToLocalDisplay | VERIFIED | `frontend/src/hooks/useEasternClock.ts` lines 50-69: `function etRunHourToLocalDisplay(now, etRunHour, tomorrow)` converts ET run hours to local browser time; lines 84-89: `computeNextUpdate` calls `etRunHourToLocalDisplay` for display |
| 7 | run_pipeline_with_retry exists in scheduler.py and wraps all 3 cron jobs | VERIFIED | `src/pipeline/scheduler.py` line 34: `def run_pipeline_with_retry(version, artifacts, pool)`; lines 173, 182, 191: all three `scheduler.add_job` calls use `run_pipeline_with_retry` as the job function |
| 8 | Retry catches urllib.error.HTTPError code 503, urllib.error.URLError, and socket.timeout | VERIFIED | `src/pipeline/scheduler.py` lines 52-53: `except urllib.error.HTTPError as exc: if exc.code != 503: raise` (only 503 retried); lines 62-63: `except (urllib.error.URLError, socket.timeout)` -- all three exception types handled |
| 9 | Retry sleeps 900 seconds (15 minutes) between attempts | VERIFIED | `src/pipeline/scheduler.py` line 31: `RETRY_SLEEP_SECONDS = 900`; line 60: `time.sleep(RETRY_SLEEP_SECONDS)` after 503; line 69: `time.sleep(RETRY_SLEEP_SECONDS)` after URLError/timeout |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/App.tsx` | pipelineTimestamp variable, initialTimestampRef, lastUpdated={pipelineTimestamp} | VERIFIED | Line 26: `pipelineTimestamp` defined from `timestampData?.timestamp`; line 29: `initialTimestampRef = useRef<string \| null>(null)`; line 78: `lastUpdated={pipelineTimestamp}` -- no `displayedTimestamp` variable exists |
| `frontend/src/hooks/useEasternClock.ts` | localTimeFmt without timeZone, etRunHourToLocalDisplay function, no ' ET' suffix | VERIFIED | Lines 20-24: `localTimeFmt` has no `timeZone` property; line 50: `function etRunHourToLocalDisplay` present; line 96: timeStr uses `tzAbbr` (browser locale), not hardcoded 'ET' |
| `src/pipeline/scheduler.py` | run_pipeline_with_retry function, RETRY_SLEEP_SECONDS = 900, all 3 add_job calls use retry wrapper | VERIFIED | Line 31: `RETRY_SLEEP_SECONDS = 900`; line 34: `def run_pipeline_with_retry`; lines 173, 182, 191: `run_pipeline_with_retry` as first arg to `scheduler.add_job` |
| `tests/test_pipeline/test_scheduler_retry.py` | 8 test methods in TestRunPipelineWithRetry class | VERIFIED | Line 42: `class TestRunPipelineWithRetry:` with 8 methods: test_success_no_retry, test_503_retries_once_and_succeeds, test_url_error_retries_once_and_succeeds, test_socket_timeout_retries_once_and_succeeds, test_503_retry_also_fails_propagates, test_404_does_not_retry, test_value_error_does_not_retry, test_url_error_retry_also_fails_propagates |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `App.tsx` | `useLatestTimestamp` | timestampData.timestamp used for pipelineTimestamp and Header lastUpdated | WIRED | App.tsx line 23: `const { data: timestampData } = useLatestTimestamp()`; line 26: `const pipelineTimestamp = timestampData?.timestamp ?? null`; line 78: `lastUpdated={pipelineTimestamp}` |
| `useEasternClock` | `localTimeFmt.format(now)` | timeStr uses browser-local formatting (no ET) | WIRED | useEasternClock.ts line 96: `const timeStr = tzAbbr ? \`${localTimeFmt.format(now)} ${tzAbbr}\` : localTimeFmt.format(now)` -- localTimeFmt has no timeZone override |
| `useEasternClock` | `etRunHourToLocalDisplay` -> `computeNextUpdate` -> nextUpdate string | WIRED | useEasternClock.ts lines 84, 89: `computeNextUpdate` calls `etRunHourToLocalDisplay(now, runHour, false/true)` to produce local-time display strings |
| `scheduler.py` | `run_pipeline_with_retry` -> `run_pipeline` (wraps the original) | WIRED | scheduler.py lines 50-70: `run_pipeline_with_retry` calls `run_pipeline(version, artifacts, pool)` in try block; retries by calling `run_pipeline()` again after sleep |
| `scheduler.py` | `add_job(run_pipeline_with_retry, ...)` for all 3 cron triggers | WIRED | scheduler.py line 173: pre_lineup job; line 182: post_lineup job; line 191: confirmation job -- all three reference `run_pipeline_with_retry` |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| BUG-A | 14.5-01-PLAN.md | Header "Last updated" shows pipeline run time, not page load time | SATISFIED | `App.tsx` line 26: `pipelineTimestamp = timestampData?.timestamp` (from /predictions/latest-timestamp); line 78: `lastUpdated={pipelineTimestamp}` wired to Header; line 29: `initialTimestampRef` tracks initial timestamp for hasNewPredictions; `displayedTimestamp` variable no longer exists in file |
| BUG-B | 14.5-02-PLAN.md | Live clock and "Next update" display in user's browser timezone | SATISFIED | `useEasternClock.ts` line 20-24: `localTimeFmt` has no `timeZone` property (uses browser default); line 50: `etRunHourToLocalDisplay` converts ET run hours to local display; line 96: timeStr uses `localTimeFmt.format(now)` with browser-local tz abbreviation, no `' ET'` concatenation |
| RETRY | 14.5-03-PLAN.md | Pipeline jobs retry once after 15 minutes on 503/timeout | SATISFIED | `scheduler.py` line 34: `run_pipeline_with_retry` catches HTTPError(503) at line 52, URLError at line 62, socket.timeout at line 62; line 31: `RETRY_SLEEP_SECONDS = 900` (15 min); lines 173/182/191: all 3 scheduler jobs reference `run_pipeline_with_retry` not `run_pipeline` |

All 3 requirements fully satisfied. No orphaned requirements.

### Anti-Patterns Found

No blockers or warnings detected.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | -- | -- | -- | -- |

Scan covered: frontend/src/App.tsx, frontend/src/hooks/useEasternClock.ts, src/pipeline/scheduler.py, tests/test_pipeline/test_scheduler_retry.py. No TODO/FIXME/PLACEHOLDER markers found in Phase 14.5 files.

### Human Verification Required

The following behaviors require visual or browser-level confirmation and cannot be verified programmatically:

#### 1. Header Timestamp Display

**Test:** Open the dashboard in a browser while the pipeline has previously run. Inspect the "Last updated" text in the header.
**Expected:** Shows the pipeline's last run time (e.g., "10:02 AM"), not the current page load time.
**Why human:** The actual displayed time depends on the /predictions/latest-timestamp endpoint returning a real pipeline run timestamp from the database.

#### 2. Browser-Local Clock

**Test:** Open the dashboard from a browser set to a non-ET timezone (e.g., PST, CST, or UTC).
**Expected:** The live clock in the header shows the browser's local time with the local timezone abbreviation (e.g., "2:34 PM PST"), not "2:34 PM ET".
**Why human:** The timezone behavior depends on the actual browser environment and its locale settings.

### Gaps Summary

None. All 9 observable truths are verified, all 4 artifacts pass existence and wiring checks, all 5 key links are confirmed wired, all 3 requirements (BUG-A, BUG-B, RETRY) are fully satisfied.

Test evidence:
- `python -m pytest tests/test_pipeline/test_scheduler_retry.py -q` -- 8 passed, 0 failed
- `npx tsc --noEmit` in frontend/ -- exit code 0, no errors

---

_Verified: 2026-04-01_
_Verifier: Claude (gsd-executor)_
