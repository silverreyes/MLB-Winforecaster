---
phase: 14.5-post-phase-14-bugfixes
plan: 02
type: execute
wave: 2
depends_on: []
autonomous: true
files_modified:
  - frontend/src/hooks/useEasternClock.ts
requirements: [BUG-B]
must_haves:
  truths:
    - "Live clock in header displays in the user's browser timezone, not hardcoded ET"
    - "'Next update' time displays in the user's browser timezone, not hardcoded ET"
    - "The ET timezone is still used internally to determine WHICH pipeline run comes next"
    - "No ' ET' suffix appended to the displayed clock time"
    - "No 'ET' label in the Next update string"
  artifacts:
    - path: "frontend/src/hooks/useEasternClock.ts"
      provides: "Browser-local clock and next-update display"
      contains: "etRunHourToLocalDisplay"
  key_links: []
---

<objective>
Fix BUG-B: Live clock and "Next update" times in the header are hardcoded to America/New_York.

Root cause: useEasternClock.ts formats clock time with timeZone: 'America/New_York' and appends ' ET', and computeNextUpdate() returns hardcoded labels like "Next update: 1:00 PM ET".

Fix: remove the ET timezone from timeFmt so the clock displays in the user's browser timezone. Update computeNextUpdate() to convert the ET-scheduled run times to the equivalent local browser times. The ET timezone is still used internally (via etPartsFmt) to determine WHICH run is next — only the display output changes.

Keep dateFmt in ET (America/New_York) — the date in the header reflects the "baseball day" context, which is ET-based.

Output: clock ticks in user's local timezone; next update countdown shows local-equivalent time; no ET labels.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Replace ET-hardcoded display with browser-local timezone in useEasternClock.ts</name>
  <files>frontend/src/hooks/useEasternClock.ts</files>
  <read_first>
    - frontend/src/hooks/useEasternClock.ts
  </read_first>
  <action>
    Read the current useEasternClock.ts. Replace its contents with the updated version below.

    Key changes:
    1. `timeFmt` — remove `timeZone: 'America/New_York'` so it formats in browser local time
    2. `timeStr` — remove the `' ET'` suffix
    3. `computeNextUpdate` — new helper `etRunHourToLocalDisplay` converts ET run hours to local browser time for display; "ET" removed from the returned string
    4. `dateFmt` — unchanged (keeps `timeZone: 'America/New_York'` for baseball-date context)

    **New useEasternClock.ts:**

    ```typescript
    import { useState, useEffect } from 'react';

    interface EasternClockState {
      dateStr: string;   // e.g., "Monday, March 30" (ET date — baseball context)
      timeStr: string;   // e.g., "2:34 PM" (browser local time, no ET label)
      nextUpdate: string; // e.g., "Next update: 1:00 PM" or "Next update: 10:00 AM tomorrow"
    }

    const PIPELINE_RUN_HOURS_ET = [10, 13, 17]; // 10 AM, 1 PM, 5 PM ET

    // ET date formatter — kept in ET for baseball-day context
    const dateFmt = new Intl.DateTimeFormat('en-US', {
      weekday: 'long',
      month: 'long',
      day: 'numeric',
      timeZone: 'America/New_York',
    });

    // Browser-local time formatter — no timeZone specified, uses browser locale
    const localTimeFmt = new Intl.DateTimeFormat('en-US', {
      hour: 'numeric',
      minute: '2-digit',
      hour12: true,
    });

    // ET hour extractor — used internally to determine which run is next
    const etHourFmt = new Intl.DateTimeFormat('en-US', {
      hour: 'numeric',
      minute: '2-digit',
      hour12: false,
      timeZone: 'America/New_York',
    });

    /**
     * Convert a pipeline run hour (in ET) to a display string in the user's browser timezone.
     *
     * Strategy: compute the current offset between local hour and ET hour, then
     * apply that offset to the target run hour to get the local equivalent.
     * This is accurate for DST transitions that happen between now and the run time
     * only in rare edge-case scenarios; for a clock display this is acceptable.
     */
    function etRunHourToLocalDisplay(now: Date, etRunHour: number, tomorrow: boolean): string {
      // Get current ET hour (24h)
      const etParts = etHourFmt.formatToParts(now);
      let currentEtHour = 0;
      for (const part of etParts) {
        if (part.type === 'hour') currentEtHour = Number(part.value);
      }

      // Local hour (24h)
      const currentLocalHour = now.getHours();
      const hourOffset = currentLocalHour - currentEtHour;

      // Build a Date representing "today (or tomorrow) at etRunHour:00 local-equivalent"
      const runDate = new Date(now);
      if (tomorrow) runDate.setDate(runDate.getDate() + 1);
      runDate.setHours(etRunHour + hourOffset, 0, 0, 0);

      return localTimeFmt.format(runDate);
    }

    function computeNextUpdate(now: Date): string {
      // Determine current ET time to pick which run is next
      const etParts = etHourFmt.formatToParts(now);
      let currentEtHour = 0;
      let currentEtMinute = 0;
      for (const part of etParts) {
        if (part.type === 'hour') currentEtHour = Number(part.value);
        if (part.type === 'minute') currentEtMinute = Number(part.value);
      }
      const currentEtTotal = currentEtHour * 60 + currentEtMinute;

      for (const runHour of PIPELINE_RUN_HOURS_ET) {
        if (runHour * 60 > currentEtTotal) {
          return 'Next update: ' + etRunHourToLocalDisplay(now, runHour, false);
        }
      }

      // Past all runs today — next is 10 AM ET tomorrow
      return 'Next update: ' + etRunHourToLocalDisplay(now, 10, true) + ' tomorrow';
    }

    function computeClock(): EasternClockState {
      const now = new Date();
      const dateStr = dateFmt.format(now);
      const timeStr = localTimeFmt.format(now);
      const nextUpdate = computeNextUpdate(now);
      return { dateStr, timeStr, nextUpdate };
    }

    export function useEasternClock(): EasternClockState {
      const [clock, setClock] = useState<EasternClockState>(computeClock);

      useEffect(() => {
        let intervalId: ReturnType<typeof setInterval> | undefined;

        const msUntilNextSecond = 1000 - (Date.now() % 1000);

        const timeoutId = setTimeout(() => {
          setClock(computeClock());

          intervalId = setInterval(() => {
            setClock(computeClock());
          }, 1000);
        }, msUntilNextSecond);

        return () => {
          clearTimeout(timeoutId);
          if (intervalId !== undefined) {
            clearInterval(intervalId);
          }
        };
      }, []);

      return clock;
    }
    ```
  </action>
  <verify>
    <automated>cd E:/ClaudeCodeProjects/MLB-WinForecaster/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/hooks/useEasternClock.ts does NOT contain `timeZone: 'America/New_York'` in timeFmt or localTimeFmt (only in dateFmt and etHourFmt)
    - frontend/src/hooks/useEasternClock.ts does NOT contain `+ ' ET'` string concatenation on timeStr
    - frontend/src/hooks/useEasternClock.ts contains `etRunHourToLocalDisplay`
    - frontend/src/hooks/useEasternClock.ts does NOT contain `'Next update: ' + RUN_LABELS` pattern
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    - Clock ticks in user's browser timezone with no ET label
    - Next update shows local-equivalent time with no ET label
    - ET timezone still used internally for run-order decisions
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` in frontend/ exits 0
</verification>

<success_criteria>
- Live clock shows user's local time
- "Next update" shows local-equivalent of the next ET pipeline run
- No "ET" label in clock or next-update display
</success_criteria>

<output>
After completion, create `.planning/phases/14.5-post-phase-14-bugfixes/14.5-02-SUMMARY.md`
</output>
