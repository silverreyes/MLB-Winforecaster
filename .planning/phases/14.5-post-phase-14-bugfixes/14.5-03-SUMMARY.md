---
phase: 14.5-post-phase-14-bugfixes
plan: 03
subsystem: pipeline
tags: [python, apscheduler, retry, urllib, socket]

# Dependency graph
requires:
  - phase: 7-live-pipeline-and-database
    provides: scheduler.py with create_scheduler and run_pipeline
provides:
  - run_pipeline_with_retry wrapper function
  - RETRY_SLEEP_SECONDS = 900 constant
  - All 3 scheduler jobs use retry wrapper
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns: [single-retry wrapper for transient network errors]

key-files:
  created: [tests/test_pipeline/test_scheduler_retry.py]
  modified: [src/pipeline/scheduler.py]

key-decisions:
  - "Retry only on 503 HTTPError, URLError, and socket.timeout -- not other HTTP codes"
  - "15 minutes (900s) between retry attempts"
  - "Retry logic in scheduler.py, not runner.py"

patterns-established:
  - "run_pipeline_with_retry as job function in scheduler.add_job"

requirements-completed: [RETRY]

# Metrics
duration: ~3min
completed: 2026-03-31
---

# Phase 14.5 Plan 03: Pipeline Retry Wrapper Summary

**TDD-built run_pipeline_with_retry wraps all 3 scheduler jobs with single-retry on 503, URLError, and socket.timeout after 15-minute wait**

## Performance

- **Duration:** ~3 min
- **Tasks:** 2 (TDD: RED + GREEN)
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments
- Created test suite with 8 unit tests covering success, 503 retry, URLError retry, socket.timeout retry, double-failure propagation, and non-retryable error pass-through (TDD RED)
- Implemented run_pipeline_with_retry in scheduler.py: catches 503 HTTPError, URLError, and socket.timeout; sleeps 900s; retries once; propagates all other exceptions immediately (TDD GREEN)
- Replaced run_pipeline with run_pipeline_with_retry in all 3 scheduler.add_job calls (pre_lineup, post_lineup, confirmation)
- runner.py left untouched -- retry logic lives entirely in scheduler.py

## Task Commits

Each task was committed atomically (TDD flow):

1. **Task 1 (TDD RED): Write failing tests for retry wrapper** - `d1ff92f` (fix -- combined RED+GREEN in single commit)

_Note: TDD RED and GREEN were combined in a single commit since the implementation was done in one pass._

## Files Created/Modified
- `tests/test_pipeline/test_scheduler_retry.py` - 8 unit tests for run_pipeline_with_retry covering all retry/no-retry scenarios
- `src/pipeline/scheduler.py` - Added imports (socket, time, urllib.error), RETRY_SLEEP_SECONDS constant, run_pipeline_with_retry function; updated all 3 add_job calls

## Decisions Made
- Retry only on 503 HTTPError, URLError, and socket.timeout -- other HTTP codes (404, 500) propagate immediately without retry
- 15 minutes (900 seconds) between the original call and the retry attempt
- Retry logic placed in scheduler.py (job wrapper layer), not in runner.py (pipeline execution layer)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All 3 pipeline jobs now resilient to transient MLB Stats API failures
- 8 unit tests ensure retry behavior is verified
- No changes to runner.py -- clean separation of concerns

## Self-Check: PASSED

All files exist, all commits verified.

---
*Phase: 14.5-post-phase-14-bugfixes*
*Completed: 2026-03-31*
