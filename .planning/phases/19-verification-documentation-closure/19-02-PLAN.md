---
phase: 19-verification-documentation-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-live-score-polling/15-VERIFICATION.md
  - .planning/phases/16-historical-game-cache/16-VERIFICATION.md
autonomous: true
requirements: [LIVE-01, LIVE-02, LIVE-03, LIVE-04, LIVE-05, LIVE-06, LIVE-07, LIVE-08, CACHE-01, CACHE-02, CACHE-03, CACHE-04, CACHE-05]
must_haves:
  truths:
    - "Phase 15 VERIFICATION.md exists with status: passed and all LIVE-01..08 marked SATISFIED"
    - "Phase 16 VERIFICATION.md exists with status: passed and all CACHE-01..05 marked SATISFIED"
    - "Each requirement's evidence cites specific file paths and line patterns from actual source code"
  artifacts:
    - path: ".planning/phases/15-live-score-polling/15-VERIFICATION.md"
      provides: "Verification report for LIVE-01 through LIVE-08"
      contains: "status: passed"
    - path: ".planning/phases/16-historical-game-cache/16-VERIFICATION.md"
      provides: "Verification report for CACHE-01 through CACHE-05"
      contains: "status: passed"
  key_links:
    - from: "15-VERIFICATION.md"
      to: "Phase 15 source files"
      via: "grep-verified evidence citations"
      pattern: "SATISFIED"
    - from: "16-VERIFICATION.md"
      to: "Phase 16 source files"
      via: "grep-verified evidence citations"
      pattern: "SATISFIED"
---

<objective>
Create VERIFICATION.md files for Phase 15 (Live Score Polling) and Phase 16 (Historical Game Cache) by verifying each requirement against the actual codebase.

Purpose: The v2.2 milestone audit identified these two phases as missing VERIFICATION.md. All code is implemented and confirmed working by the audit's integration check -- this plan produces the formal verification documentation.

Output: Two VERIFICATION.md files with requirement-by-requirement evidence from source code.
</objective>

<execution_context>
@C:/Users/silve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/silve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v2.2-MILESTONE-AUDIT.md
@.planning/phases/15-live-score-polling/15-01-SUMMARY.md
@.planning/phases/15-live-score-polling/15-02-SUMMARY.md
@.planning/phases/15-live-score-polling/15-03-SUMMARY.md
@.planning/phases/15-live-score-polling/15-04-SUMMARY.md
@.planning/phases/16-historical-game-cache/16-01-SUMMARY.md
@.planning/phases/16-historical-game-cache/16-02-SUMMARY.md
@.planning/phases/16-historical-game-cache/16-03-SUMMARY.md
@.planning/phases/13-schema-migration-and-game-visibility/13-VERIFICATION.md

<interfaces>
<!-- Reference VERIFICATION.md format from Phase 13 (13-VERIFICATION.md). Follow the same structure:
     - YAML frontmatter with phase, verified, status, score, re_verification
     - Observable Truths table with # / Truth / Status / Evidence columns
     - Required Artifacts table
     - Key Link Verification table
     - Requirements Coverage table with Requirement / Source Plan / Description / Status / Evidence
     - Anti-Patterns Found table
     - Human Verification Required section
     - Gaps Summary section
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 15 VERIFICATION.md (LIVE-01 through LIVE-08)</name>
  <files>.planning/phases/15-live-score-polling/15-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/13-schema-migration-and-game-visibility/13-VERIFICATION.md
    - .planning/phases/15-live-score-polling/15-01-SUMMARY.md
    - .planning/phases/15-live-score-polling/15-02-SUMMARY.md
    - .planning/phases/15-live-score-polling/15-03-SUMMARY.md
    - .planning/phases/15-live-score-polling/15-04-SUMMARY.md
    - .planning/REQUIREMENTS.md
    - api/models.py
    - api/routes/games.py
    - src/data/mlb_schedule.py
    - src/pipeline/db.py
    - src/pipeline/scheduler.py
    - frontend/src/api/types.ts
    - frontend/src/hooks/useGames.ts
    - frontend/src/components/GameCard.tsx
    - frontend/src/components/BasesDiamond.tsx
    - tests/test_api/test_games_live.py
    - tests/test_pipeline/test_live_poller.py
  </read_first>
  <action>
    Create `.planning/phases/15-live-score-polling/15-VERIFICATION.md` following the exact format of 13-VERIFICATION.md.

    **Frontmatter:**
    ```yaml
    ---
    phase: 15-live-score-polling
    verified: 2026-04-01T00:00:00Z
    status: passed
    score: "N/N must-haves verified"
    re_verification: false
    ---
    ```

    **Phase Goal:** "Users see real-time game progress on in-progress cards and the system automatically records outcomes when games finish"

    **Observable Truths to verify (minimum 12):**
    1. LiveScoreData Pydantic model exists with 14 typed fields (home_score, away_score, inning, inning_half, outs, balls, strikes, on_first, on_second, on_third, batter_name, batter_stats, ondeck_name, pitcher_name)
    2. GameResponse has an optional live_score field of type LiveScoreData
    3. get_linescore_cached() uses a thread-safe dict+Lock cache with 90s TTL and 20-entry max
    4. parse_linescore() extracts live game state from raw MLB API response
    5. statsapi.get('game') uses fields parameter to reduce response size
    6. In-progress game cards display current score and inning (ScoreRow component)
    7. Dashboard polls live game data every 90 seconds only when hasLiveGames AND view_mode is "live" (dual gate)
    8. User can expand an in-progress card (ScoreRow has expand/collapse via useState)
    9. Expanded card shows BasesDiamond SVG with amber-highlighted runners
    10. Expanded card shows pitch count (balls/strikes/outs)
    11. Expanded card shows current batter name and stats
    12. Expanded card shows on-deck batter name
    13. write_game_outcome() writes actual_winner and prediction_correct to predictions table
    14. live_poller_job detects Final transition and calls write_game_outcome

    **For each truth:** grep the source files to find the exact line(s). Record file:line as evidence.

    **Required Artifacts:**
    - `api/models.py` — LiveScoreData class with 14 fields, GameResponse.live_score Optional field
    - `src/data/mlb_schedule.py` — get_linescore_cached(), parse_linescore(), _linescore_cache dict, _linescore_lock Lock
    - `api/routes/games.py` — Live score enrichment in build_games_response or endpoint handler
    - `src/pipeline/db.py` — write_game_outcome() function
    - `src/pipeline/scheduler.py` — live_poller_job with IntervalTrigger
    - `frontend/src/api/types.ts` — LiveScoreData TypeScript interface
    - `frontend/src/hooks/useGames.ts` — 90s dual-gate polling (hasLiveGames + viewMode === 'live')
    - `frontend/src/components/GameCard.tsx` — ScoreRow, expand/collapse logic, conditional render on game_status === 'LIVE'
    - `frontend/src/components/BasesDiamond.tsx` — SVG diamond with on_first/on_second/on_third highlighting
    - `tests/test_api/test_games_live.py` — 12 tests for parse_linescore and get_linescore_cached
    - `tests/test_pipeline/test_live_poller.py` — 8 tests for write_game_outcome and live_poller_job

    **Key Links:**
    - mlb_schedule.py parse_linescore -> LiveScoreData model fields
    - games.py route handler -> get_linescore_cached per LIVE game -> GameResponse.live_score
    - scheduler.py live_poller_job -> write_game_outcome on Final transition
    - useGames.ts refetchInterval callback -> hasLiveGames + viewMode dual gate
    - GameCard.tsx -> game.live_score data -> ScoreRow/LiveDetail/BasesDiamond rendering
    - App.tsx -> GameCardGrid -> GameCard with viewMode prop

    **Requirements Coverage (8 entries):**
    - LIVE-01: Score + inning on in-progress card — verify ScoreRow renders home_score, away_score, inning, inning_half
    - LIVE-02: 90s polling gate — verify refetchInterval uses 90_000 with dual gate (hasLiveGames AND viewMode === 'live')
    - LIVE-03: Expand in-progress card — verify useState expand/collapse in ScoreRow or GameCard for LIVE cards
    - LIVE-04: Bases diamond — verify BasesDiamond.tsx renders SVG with on_first/on_second/on_third props
    - LIVE-05: Pitch count — verify LiveDetail or expanded section renders balls, strikes, outs
    - LIVE-06: Batter stats — verify LiveDetail renders batter_name and batter_stats
    - LIVE-07: On-deck batter — verify LiveDetail renders ondeck_name
    - LIVE-08: Final transition writes outcome — verify write_game_outcome sets actual_winner + prediction_correct; verify live_poller_job calls it

    Run `python -m pytest tests/test_api/test_games_live.py tests/test_pipeline/test_live_poller.py -q` and record the pass count. Run `cd frontend && npx tsc --noEmit` and record exit code.
  </action>
  <verify>
    <automated>grep -c "SATISFIED" ".planning/phases/15-live-score-polling/15-VERIFICATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/15-live-score-polling/15-VERIFICATION.md exists
    - File contains `status: passed` in YAML frontmatter
    - File contains "LIVE-01" through "LIVE-08" each with "SATISFIED" status
    - File contains an "Observable Truths" table with at least 12 entries all marked VERIFIED
    - File contains a "Required Artifacts" table with at least 10 entries all marked VERIFIED
    - File contains a "Key Link Verification" table with at least 5 entries all marked WIRED
    - File contains a "Requirements Coverage" table with exactly 8 rows (LIVE-01 through LIVE-08)
    - Each requirement row includes a Source Plan column citing 15-01, 15-02, or 15-03
    - Evidence cites specific file paths and line patterns (not vague references)
  </acceptance_criteria>
  <done>
    Phase 15 VERIFICATION.md exists with status: passed, all 8 LIVE requirements marked SATISFIED with grep-verified evidence from source code
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 16 VERIFICATION.md (CACHE-01 through CACHE-05)</name>
  <files>.planning/phases/16-historical-game-cache/16-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/13-schema-migration-and-game-visibility/13-VERIFICATION.md
    - .planning/phases/16-historical-game-cache/16-01-SUMMARY.md
    - .planning/phases/16-historical-game-cache/16-02-SUMMARY.md
    - .planning/phases/16-historical-game-cache/16-03-SUMMARY.md
    - .planning/REQUIREMENTS.md
    - src/pipeline/migration_002.sql
    - src/pipeline/db.py
    - src/pipeline/runner.py
    - src/features/feature_builder.py
    - src/pipeline/live_features.py
    - scripts/seed_game_logs.py
    - tests/test_pipeline/test_game_log_sync.py
    - tests/test_pipeline/conftest.py
  </read_first>
  <action>
    Create `.planning/phases/16-historical-game-cache/16-VERIFICATION.md` following the exact format of 13-VERIFICATION.md.

    **Frontmatter:**
    ```yaml
    ---
    phase: 16-historical-game-cache
    verified: 2026-04-01T00:00:00Z
    status: passed
    score: "N/N must-haves verified"
    re_verification: false
    ---
    ```

    **Phase Goal:** "The pipeline never re-fetches immutable completed game data from the MLB Stats API; instead it reads from a local game_logs Postgres table, seeded once and grown incrementally"

    **Observable Truths to verify (minimum 10):**
    1. game_logs table has columns: game_id (VARCHAR PK), game_date, home_team, away_team, home_score, away_score, winner, plus stats columns
    2. migration_002.sql creates game_logs with CREATE TABLE IF NOT EXISTS (idempotent)
    3. migration_002.sql creates at least 3 indexes on game_logs
    4. apply_schema() in db.py executes migration_002.sql after schema.sql
    5. batch_insert_game_logs() uses INSERT ... ON CONFLICT DO NOTHING (immutability)
    6. sync_game_logs() queries MAX(game_date), fetches from max-1 day to yesterday
    7. seed_game_logs.py script backfills 2025 and 2026 completed games
    8. run_pipeline.py calls sync_game_logs at startup
    9. FeatureBuilder accepts pool parameter (default None for backward compat)
    10. FeatureBuilder._load_from_game_logs queries game_logs table when pool provided
    11. LiveFeatureBuilder passes pool to FeatureBuilder constructor
    12. runner.py passes pool to LiveFeatureBuilder

    **For each truth:** grep the source files to find the exact line(s). Record file:line as evidence.

    **Required Artifacts:**
    - `src/pipeline/migration_002.sql` — CREATE TABLE game_logs, indexes, idempotent DDL
    - `src/pipeline/db.py` — batch_insert_game_logs(), sync_game_logs(), apply_schema with migration_002
    - `scripts/seed_game_logs.py` — CLI seed script for 2025+2026 backfill
    - `src/pipeline/runner.py` — sync_game_logs call, pool wiring to LiveFeatureBuilder
    - `src/features/feature_builder.py` — pool parameter, _load_from_game_logs method
    - `src/pipeline/live_features.py` — LiveFeatureBuilder pool passthrough
    - `tests/test_pipeline/test_game_log_sync.py` — Tests for schema, insert, immutability, sync
    - `tests/test_pipeline/conftest.py` — DROP TABLE IF EXISTS game_logs in clean_tables

    **Key Links:**
    - db.py apply_schema -> migration_002.sql execution
    - db.py sync_game_logs -> batch_insert_game_logs -> ON CONFLICT DO NOTHING
    - runner.py -> sync_game_logs(pool) at pipeline startup
    - runner.py -> LiveFeatureBuilder(pool=pool) -> FeatureBuilder(pool=pool) -> _load_from_game_logs
    - seed_game_logs.py -> batch_insert_game_logs for backfill

    **Requirements Coverage (5 entries):**
    - CACHE-01: game_logs table exists with required columns — verify migration_002.sql DDL has game_id, game_date, home_team, away_team, home_score, away_score, winner columns
    - CACHE-02: One-time seed job backfills 2025+2026 — verify seed_game_logs.py has --seasons argument accepting year list, calls batch_insert_game_logs
    - CACHE-03: Incremental fetch from last known date — verify sync_game_logs queries MAX(game_date), fetches only from that date forward
    - CACHE-04: FeatureBuilder reads from game_logs — verify _load_from_game_logs method queries game_logs table, verify pool parameter gates DB vs API path
    - CACHE-05: Immutability via ON CONFLICT DO NOTHING — verify batch_insert_game_logs SQL contains ON CONFLICT (game_id) DO NOTHING

    Run `python -m pytest tests/test_pipeline/test_game_log_sync.py -q` and record the pass count.
  </action>
  <verify>
    <automated>grep -c "SATISFIED" ".planning/phases/16-historical-game-cache/16-VERIFICATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/16-historical-game-cache/16-VERIFICATION.md exists
    - File contains `status: passed` in YAML frontmatter
    - File contains "CACHE-01" through "CACHE-05" each with "SATISFIED" status
    - File contains an "Observable Truths" table with at least 10 entries all marked VERIFIED
    - File contains a "Required Artifacts" table with at least 7 entries all marked VERIFIED
    - File contains a "Key Link Verification" table with at least 4 entries all marked WIRED
    - File contains a "Requirements Coverage" table with exactly 5 rows (CACHE-01 through CACHE-05)
    - Each requirement row includes a Source Plan column citing 16-01, 16-02, or 16-03
    - Evidence cites specific file paths and line patterns (not vague references)
  </acceptance_criteria>
  <done>
    Phase 16 VERIFICATION.md exists with status: passed, all 5 CACHE requirements marked SATISFIED with grep-verified evidence from source code
  </done>
</task>

</tasks>

<verification>
- `grep "status: passed" .planning/phases/15-live-score-polling/15-VERIFICATION.md` returns a match
- `grep "status: passed" .planning/phases/16-historical-game-cache/16-VERIFICATION.md` returns a match
- `grep -c "SATISFIED" .planning/phases/15-live-score-polling/15-VERIFICATION.md` returns 8 or more
- `grep -c "SATISFIED" .planning/phases/16-historical-game-cache/16-VERIFICATION.md` returns 5 or more
</verification>

<success_criteria>
- Phase 15 VERIFICATION.md confirms LIVE-01 through LIVE-08 all SATISFIED
- Phase 16 VERIFICATION.md confirms CACHE-01 through CACHE-05 all SATISFIED
- Both files follow the exact structure of 13-VERIFICATION.md
- Evidence in each row traces to actual source file paths and line-level patterns
</success_criteria>

<output>
After completion, create `.planning/phases/19-verification-documentation-closure/19-02-SUMMARY.md`
</output>
