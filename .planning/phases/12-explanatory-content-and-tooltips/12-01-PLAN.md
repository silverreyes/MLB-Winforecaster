---
phase: 12-explanatory-content-and-tooltips
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/AboutModels.tsx
  - frontend/src/components/AboutModels.module.css
  - frontend/src/components/Tooltip.tsx
  - frontend/src/components/Tooltip.module.css
  - frontend/src/components/EdgeBadge.tsx
  - frontend/src/components/EdgeBadge.module.css
  - frontend/src/App.tsx
autonomous: true
requirements: [EXPLAIN-01, EXPLAIN-02, EXPLAIN-03, EXPLAIN-04, EXPLAIN-05, EXPLAIN-06, EXPLAIN-07, TLTP-01, TLTP-02]

must_haves:
  truths:
    - "Dashboard has a collapsible 'About the Models' section that defaults to collapsed"
    - "Clicking the section header expands/collapses the explanatory content"
    - "Expanded content explains all three model types (LR, RF, XGBoost) in plain English"
    - "Content explains probability interpretation, calibration, PRE vs POST-LINEUP, and Kalshi mechanics"
    - "Kalshi section discloses 7% fee and contains no trading recommendations"
    - "Buy Yes edge badge has a (?) icon that shows tooltip on hover/focus"
    - "Buy No edge badge has a (?) icon that shows tooltip on hover/focus"
  artifacts:
    - path: "frontend/src/components/AboutModels.tsx"
      provides: "Collapsible explanatory section using native <details>/<summary>"
      exports: ["AboutModels"]
    - path: "frontend/src/components/AboutModels.module.css"
      provides: "Styles for collapsible section matching dark amber design system"
      contains: "details[open] .chevron"
    - path: "frontend/src/components/Tooltip.tsx"
      provides: "Reusable CSS tooltip wrapper component"
      exports: ["Tooltip"]
    - path: "frontend/src/components/Tooltip.module.css"
      provides: "Tooltip positioning, visibility, and mobile responsive styles"
      contains: ".wrapper:hover .tip"
    - path: "frontend/src/components/EdgeBadge.tsx"
      provides: "Edge badge with inline tooltip icons"
      contains: "Tooltip"
    - path: "frontend/src/App.tsx"
      provides: "AboutModels wired between AccuracyStrip and NewPredictionsBanner"
      contains: "AboutModels"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/AboutModels.tsx"
      via: "import { AboutModels }"
      pattern: "import.*AboutModels.*from"
    - from: "frontend/src/components/EdgeBadge.tsx"
      to: "frontend/src/components/Tooltip.tsx"
      via: "import { Tooltip }"
      pattern: "import.*Tooltip.*from"
    - from: "frontend/src/components/AboutModels.tsx"
      to: "frontend/src/components/AboutModels.module.css"
      via: "CSS module import"
      pattern: "import styles from.*AboutModels.module.css"
---

<objective>
Add explanatory UI content and inline tooltips to the MLB Win Probability dashboard.

Purpose: Users currently see model predictions, Kalshi prices, and edge signals but have no on-page explanation of what these mean. This plan adds a collapsible "About the Models" section explaining all model types, probability interpretation, calibration, PRE/POST-LINEUP, and Kalshi mechanics (with 7% fee disclosure and no trading advice). It also adds (?) tooltip icons on Buy Yes/No badges explaining contract mechanics.

Output: 4 new files (AboutModels component + CSS, Tooltip component + CSS), 3 modified files (EdgeBadge.tsx, EdgeBadge.module.css, App.tsx). All 9 requirements (EXPLAIN-01 through EXPLAIN-07, TLTP-01, TLTP-02) addressed.
</objective>

<execution_context>
@C:/Users/silve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/silve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-explanatory-content-and-tooltips/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/components/EdgeBadge.tsx (CURRENT STATE -- will be modified):
```typescript
import styles from './EdgeBadge.module.css';

interface EdgeBadgeProps {
  signal: 'BUY_YES' | 'BUY_NO';
  magnitude: number;
}

export function EdgeBadge({ signal, magnitude }: EdgeBadgeProps) {
  const isBuyYes = signal === 'BUY_YES';
  const text = isBuyYes
    ? `BUY YES +${magnitude.toFixed(1)}pts`
    : `BUY NO -${Math.abs(magnitude).toFixed(1)}pts`;

  return (
    <span className={`${styles.badge} ${isBuyYes ? styles.buyYes : styles.buyNo}`}>
      {text}
    </span>
  );
}
```

From frontend/src/App.tsx (CURRENT RENDER ORDER):
```typescript
<Header ... />
<AccuracyStrip />
<NewPredictionsBanner ... />
<main>...</main>
```

From frontend/src/index.css (CSS CUSTOM PROPERTIES):
```css
:root {
  --color-bg: #0A0A0F;
  --color-surface: #12121A;
  --color-border: #1E1E2A;
  --color-text-primary: #E8E8ED;
  --color-text-secondary: #8A8A9A;
  --color-accent: #F59E0B;
  --color-accent-muted: #D97706;
  --color-edge-green: #22C55E;
  --color-edge-red: #EF4444;
  --color-stale: #6B7280;
  --space-xs: 4px; --space-sm: 8px; --space-md: 16px;
  --space-lg: 24px; --space-xl: 32px; --space-2xl: 48px;
  --font-data: 'DM Mono', monospace;
  --font-ui: 'DM Sans', sans-serif;
}
```

From frontend/src/components/EdgeBadge.module.css (CURRENT STYLES):
```css
.badge {
  display: inline-flex;
  align-items: center;
  font-family: var(--font-data);
  font-size: 12px;
  font-weight: 400;
  padding: var(--space-xs) var(--space-sm);
  border-radius: 4px;
  font-variant-numeric: tabular-nums;
  line-height: 1.3;
  white-space: nowrap;
}
.buyYes { color: var(--color-edge-green); background: rgba(34, 197, 94, 0.12); }
.buyNo { color: var(--color-edge-red); background: rgba(239, 68, 68, 0.12); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AboutModels collapsible section and wire into App</name>
  <files>
    frontend/src/components/AboutModels.tsx,
    frontend/src/components/AboutModels.module.css,
    frontend/src/App.tsx
  </files>
  <read_first>
    frontend/src/App.tsx,
    frontend/src/index.css,
    frontend/src/components/AccuracyStrip.tsx,
    frontend/src/components/AccuracyStrip.module.css,
    .planning/phases/12-explanatory-content-and-tooltips/12-RESEARCH.md
  </read_first>
  <action>
**Create `frontend/src/components/AboutModels.tsx`** with the following exact structure:

- Named export: `export function AboutModels()`
- Import CSS module: `import styles from './AboutModels.module.css';`
- Use native `<details>` / `<summary>` elements (NOT useState for open/close)
- Outer wrapper: `<div className={styles.container}>`
- `<details className={styles.details}>` (no `open` attribute -- defaults collapsed)
- `<summary className={styles.summary}>` containing:
  - `<span className={styles.summaryText}>About the Models</span>`
  - `<span className={styles.chevron} aria-hidden="true">{'\u25B6'}</span>` (Unicode right-pointing triangle)
- Content `<div className={styles.content}>` with 5 sections, each wrapped in `<section className={styles.section}>`:

**Section 1 - Model Types** (EXPLAIN-02):
- Heading: `<h3 className={styles.heading}>Model Types</h3>`
- `<ul className={styles.list}>` with 3 `<li>` items:
  - `<strong>Logistic Regression (LR)</strong>` followed by ` — A linear model that weights each stat (win rate, ERA, etc.) to estimate win probability. Simple and interpretable.`
  - `<strong>Random Forest (RF)</strong>` followed by ` — Builds hundreds of decision trees on random subsets of the data, then averages their predictions. Handles complex stat interactions.`
  - `<strong>XGBoost (XGB)</strong>` followed by ` — Builds decision trees sequentially, with each tree correcting the previous ones' errors. Often the most accurate on structured data.`
- After the list, a paragraph: `<p className={styles.paragraph}>The <strong>ensemble</strong> probability shown on each card averages all three models for a more stable estimate.</p>`

**Section 2 - Reading the Probabilities** (EXPLAIN-03):
- Heading: `<h3 className={styles.heading}>Reading the Probabilities</h3>`
- Paragraph: `A probability of <strong>68%</strong> means the home team wins in roughly 68 out of 100 similar matchups.`

**Section 3 - Calibration** (EXPLAIN-04):
- Heading: `<h3 className={styles.heading}>Calibration</h3>`
- Paragraph: `These models are <em>calibrated</em>: when they say 70%, the home team has historically won about 70% of the time. The Brier scores above measure calibration accuracy (lower is better).`

**Section 4 - PRE-LINEUP vs. POST-LINEUP** (EXPLAIN-05):
- Heading: `<h3 className={styles.heading}>PRE-LINEUP vs. POST-LINEUP</h3>`
- Paragraph containing: `<strong>PRE-LINEUP</strong> predictions use only team-level stats (win rate, run differential, etc.) and carry more uncertainty. <strong>POST-LINEUP</strong> predictions incorporate confirmed starting pitcher data and are the primary signal.`

**Section 5 - Kalshi Market Prices** (EXPLAIN-06, EXPLAIN-07):
- Heading: `<h3 className={styles.heading}>Kalshi Market Prices</h3>`
- First paragraph (`className={styles.paragraph}`): `Kalshi is a regulated prediction market where contracts pay $1 if an outcome occurs. A price of 62c implies the market estimates a 62% chance the home team wins. The edge signal compares model probability against market price.`
- Second paragraph (`className={styles.disclaimer}`): `Kalshi charges a 7% fee on net profits. Displayed prices do not account for this fee. Nothing on this dashboard is trading advice or a recommendation to buy or sell contracts.`

**CRITICAL for EXPLAIN-07:** The disclaimer paragraph must:
1. Mention "7% fee"
2. Include "Nothing on this dashboard is trading advice or a recommendation to buy or sell contracts"
3. NOT contain any action verbs directed at the user (no "you should", "consider buying", "take advantage", etc.)

**Create `frontend/src/components/AboutModels.module.css`** with these exact styles:

```css
.container {
  background: var(--color-surface);
  border-bottom: 1px solid var(--color-border);
}

.details {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 var(--space-lg);
}

.summary {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  padding: var(--space-sm) 0;
  cursor: pointer;
  list-style: none;
  user-select: none;
}

.summary::-webkit-details-marker {
  display: none;
}

.summaryText {
  font-family: var(--font-ui);
  font-size: 12px;
  font-weight: 600;
  color: var(--color-text-secondary);
}

.chevron {
  font-size: 8px;
  color: var(--color-text-secondary);
  transition: transform 0.2s ease;
}

.details[open] .chevron {
  transform: rotate(90deg);
}

.content {
  padding: 0 0 var(--space-md);
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}

.section {
  display: flex;
  flex-direction: column;
  gap: var(--space-xs);
}

.heading {
  font-family: var(--font-ui);
  font-size: 13px;
  font-weight: 600;
  color: var(--color-accent);
  margin: 0;
}

.list {
  margin: 0;
  padding-left: var(--space-lg);
  display: flex;
  flex-direction: column;
  gap: var(--space-xs);
}

.list li {
  font-family: var(--font-ui);
  font-size: 13px;
  color: var(--color-text-secondary);
  line-height: 1.5;
}

.paragraph {
  font-family: var(--font-ui);
  font-size: 13px;
  color: var(--color-text-secondary);
  line-height: 1.5;
  margin: 0;
}

.disclaimer {
  font-family: var(--font-ui);
  font-size: 12px;
  color: var(--color-stale);
  line-height: 1.5;
  margin: 0;
  font-style: italic;
}
```

**Modify `frontend/src/App.tsx`:**
- Add import: `import { AboutModels } from './components/AboutModels';`
- Insert `<AboutModels />` between `<AccuracyStrip />` and `<NewPredictionsBanner ... />` in the JSX render. The final order must be:
  ```
  <Header ... />
  <AccuracyStrip />
  <AboutModels />
  <NewPredictionsBanner ... />
  <main>...</main>
  ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/AboutModels.tsx` contains `export function AboutModels`
    - `AboutModels.tsx` contains `<details` and `<summary`
    - `AboutModels.tsx` contains `Logistic Regression` and `Random Forest` and `XGBoost`
    - `AboutModels.tsx` contains `68 out of 100`
    - `AboutModels.tsx` contains `calibrated`
    - `AboutModels.tsx` contains `PRE-LINEUP` and `POST-LINEUP`
    - `AboutModels.tsx` contains `7% fee`
    - `AboutModels.tsx` contains `Nothing on this dashboard is trading advice`
    - `AboutModels.tsx` does NOT contain `you should` or `consider buying` or `take advantage`
    - `frontend/src/components/AboutModels.module.css` contains `details[open] .chevron`
    - `AboutModels.module.css` contains `list-style: none`
    - `AboutModels.module.css` contains `::-webkit-details-marker`
    - `AboutModels.module.css` contains `var(--color-accent)` (heading color)
    - `AboutModels.module.css` contains `var(--color-stale)` (disclaimer color)
    - `frontend/src/App.tsx` contains `import { AboutModels }`
    - `frontend/src/App.tsx` contains `<AboutModels />`
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Dashboard has a collapsible "About the Models" section (defaults collapsed) between AccuracyStrip and NewPredictionsBanner. Expanded section contains all required explanatory text covering model types, probability interpretation, calibration, PRE/POST-LINEUP, and Kalshi mechanics with 7% fee disclosure and no trading advice. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Tooltip component and add (?) icons to EdgeBadge</name>
  <files>
    frontend/src/components/Tooltip.tsx,
    frontend/src/components/Tooltip.module.css,
    frontend/src/components/EdgeBadge.tsx,
    frontend/src/components/EdgeBadge.module.css
  </files>
  <read_first>
    frontend/src/components/EdgeBadge.tsx,
    frontend/src/components/EdgeBadge.module.css,
    frontend/src/components/KalshiSection.tsx,
    frontend/src/components/KalshiSection.module.css,
    .planning/phases/12-explanatory-content-and-tooltips/12-RESEARCH.md
  </read_first>
  <action>
**Create `frontend/src/components/Tooltip.tsx`** with the following exact implementation:

```typescript
import styles from './Tooltip.module.css';

interface TooltipProps {
  text: string;
  children?: React.ReactNode;
}

export function Tooltip({ text, children }: TooltipProps) {
  return (
    <span className={styles.wrapper} tabIndex={0} role="button" aria-label={text}>
      {children ?? <span className={styles.icon}>?</span>}
      <span className={styles.tip} role="tooltip">{text}</span>
    </span>
  );
}
```

- Named export: `export function Tooltip`
- `tabIndex={0}` for keyboard/touch focus
- `role="button"` and `aria-label={text}` for accessibility
- Default children renders a `?` icon in a circle
- `role="tooltip"` on the tip span for semantics

**Create `frontend/src/components/Tooltip.module.css`** with these exact styles:

```css
.wrapper {
  position: relative;
  display: inline-flex;
  align-items: center;
  cursor: help;
}

.wrapper:focus {
  outline: none;
}

.wrapper:focus-visible {
  outline: 1px solid var(--color-accent);
  outline-offset: 2px;
  border-radius: 50%;
}

.icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 14px;
  height: 14px;
  border-radius: 50%;
  border: 1px solid var(--color-text-secondary);
  font-family: var(--font-ui);
  font-size: 10px;
  font-weight: 600;
  color: var(--color-text-secondary);
  line-height: 1;
  flex-shrink: 0;
}

.tip {
  position: absolute;
  bottom: calc(100% + 6px);
  left: 50%;
  transform: translateX(-50%);
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  color: var(--color-text-secondary);
  font-family: var(--font-ui);
  font-size: 12px;
  line-height: 1.4;
  padding: var(--space-xs) var(--space-sm);
  border-radius: 4px;
  max-width: 220px;
  white-space: normal;
  pointer-events: none;
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.15s ease;
  z-index: 10;
}

.wrapper:hover .tip,
.wrapper:focus-visible .tip {
  opacity: 1;
  visibility: visible;
}

@media (max-width: 768px) {
  .tip {
    left: auto;
    right: 0;
    transform: none;
  }
}
```

Key CSS details:
- `.tip` uses `visibility: hidden` + `opacity: 0` (not just `opacity: 0`) so it doesn't intercept pointer events
- `.wrapper:hover .tip` and `.wrapper:focus-visible .tip` both set `opacity: 1` and `visibility: visible`
- Mobile breakpoint at 768px shifts tooltip anchor to right-aligned to prevent viewport overflow
- Background uses `var(--color-bg)` (#0A0A0F), border uses `var(--color-border)` (#1E1E2A)

**Modify `frontend/src/components/EdgeBadge.tsx`** to add tooltip icons. Replace the entire file with:

```typescript
import { Tooltip } from './Tooltip';
import styles from './EdgeBadge.module.css';

interface EdgeBadgeProps {
  signal: 'BUY_YES' | 'BUY_NO';
  magnitude: number;
}

const TOOLTIP_YES = 'Pays $1 if the home team wins. You pay the displayed price.';
const TOOLTIP_NO = 'Pays $1 if the home team loses. You pay 1 minus the Yes price.';

export function EdgeBadge({ signal, magnitude }: EdgeBadgeProps) {
  const isBuyYes = signal === 'BUY_YES';
  const text = isBuyYes
    ? `BUY YES +${magnitude.toFixed(1)}pts`
    : `BUY NO -${Math.abs(magnitude).toFixed(1)}pts`;
  const tooltipText = isBuyYes ? TOOLTIP_YES : TOOLTIP_NO;

  return (
    <span className={`${styles.badge} ${isBuyYes ? styles.buyYes : styles.buyNo}`}>
      {text}
      <Tooltip text={tooltipText} />
    </span>
  );
}
```

Key changes to EdgeBadge:
- Add `import { Tooltip } from './Tooltip';` at top
- Define two constants: `TOOLTIP_YES` with exact text `'Pays $1 if the home team wins. You pay the displayed price.'` and `TOOLTIP_NO` with exact text `'Pays $1 if the home team loses. You pay 1 minus the Yes price.'`
- Add `const tooltipText = isBuyYes ? TOOLTIP_YES : TOOLTIP_NO;`
- Add `<Tooltip text={tooltipText} />` inside the badge span, after `{text}`

**Modify `frontend/src/components/EdgeBadge.module.css`** to add a gap for the tooltip icon. Add `gap: var(--space-xs);` to the existing `.badge` rule. The `.badge` rule should become:

```css
.badge {
  display: inline-flex;
  align-items: center;
  gap: var(--space-xs);
  font-family: var(--font-data);
  font-size: 12px;
  font-weight: 400;
  padding: var(--space-xs) var(--space-sm);
  border-radius: 4px;
  font-variant-numeric: tabular-nums;
  line-height: 1.3;
  white-space: nowrap;
}
```

Only change: addition of `gap: var(--space-xs);` (4px gap between badge text and (?) icon).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/Tooltip.tsx` contains `export function Tooltip`
    - `Tooltip.tsx` contains `role="tooltip"`
    - `Tooltip.tsx` contains `tabIndex={0}`
    - `Tooltip.tsx` contains `aria-label={text}`
    - `frontend/src/components/Tooltip.module.css` contains `.wrapper:hover .tip`
    - `Tooltip.module.css` contains `.wrapper:focus-visible .tip`
    - `Tooltip.module.css` contains `visibility: hidden`
    - `Tooltip.module.css` contains `max-width: 220px`
    - `Tooltip.module.css` contains `@media (max-width: 768px)`
    - `frontend/src/components/EdgeBadge.tsx` contains `import { Tooltip } from './Tooltip'`
    - `EdgeBadge.tsx` contains `Pays $1 if the home team wins`
    - `EdgeBadge.tsx` contains `Pays $1 if the home team loses`
    - `EdgeBadge.tsx` contains `<Tooltip text={tooltipText} />`
    - `frontend/src/components/EdgeBadge.module.css` contains `gap: var(--space-xs)`
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    Tooltip component exists with CSS hover/focus visibility, keyboard accessibility (tabIndex, aria-label), and mobile-responsive positioning. EdgeBadge renders a (?) icon after the BUY YES / BUY NO text that shows a tooltip explaining contract mechanics on hover or focus. Full Vite production build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- TypeScript compiles with zero errors
2. `cd frontend && npm run build` -- Vite production build succeeds
3. All 4 new files exist: AboutModels.tsx, AboutModels.module.css, Tooltip.tsx, Tooltip.module.css
4. AboutModels is rendered between AccuracyStrip and NewPredictionsBanner in App.tsx
5. EdgeBadge imports and renders Tooltip component
6. AboutModels content includes all required text: model descriptions, probability interpretation, calibration, PRE/POST-LINEUP, Kalshi mechanics, 7% fee disclosure
7. No trading advice language in Kalshi section (no "you should", "consider buying", etc.)
8. Tooltip text matches requirements: "Pays $1 if the home team wins/loses" with price explanation
</verification>

<success_criteria>
- Dashboard includes a collapsible "About the Models" section that defaults to collapsed and toggles on click
- Expanded section contains plain-English explanations of LR, RF, XGBoost, probability, calibration, PRE/POST-LINEUP, and Kalshi
- Kalshi section discloses 7% fee and contains zero trading recommendations
- Buy Yes badge has inline (?) with tooltip: "Pays $1 if the home team wins. You pay the displayed price."
- Buy No badge has inline (?) with tooltip: "Pays $1 if the home team loses. You pay 1 minus the Yes price."
- TypeScript compiles cleanly and Vite build succeeds
- Mobile layout does not break (768px breakpoint handling for tooltips)
</success_criteria>

<output>
After completion, create `.planning/phases/12-explanatory-content-and-tooltips/12-01-SUMMARY.md`
</output>
